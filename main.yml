name:workflow with multiple satges
on:push
 jobs: job-on-ubuntu:
 steps:
 -run:date
 -run:pwd
 job_on_windows:
 name:a job running on windows
 runs-on:windows-latest
 steps:
 -name:first step in windows job
 run:date
 -run:ls-la
 dependent-job-1:
 needs:[jon-on-ubuntu,job-on-windows]
 runs-on;macos-latest
 name:a job that runs on macos after first two jobs
 steps:
 -run:pwd
 -name: new step run:ls
 -run:env
